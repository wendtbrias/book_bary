<% if(locals.error != null && locals.error != undefined) { %>
     <div class="alert w-[50%] mt-7 py-2 px-3 rounded-md bg-blue-100 text-blue-500 flex items-center justify-between">
         <h2 class="text-lg font-medium"><%= locals.error %></h2>
         <a href="/book/add" class="underline font-semibold">X</a>
     </div>
<% } %>